import styles from './Footer.module.css';

const Footer = () => {
    return (
        <footer className={styles.tt}>
            <nav>

            </nav>
        </footer>
    )
}

export default Footer;