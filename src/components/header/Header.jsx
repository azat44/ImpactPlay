import styles from './Footer.module.css';

const Header = () => {
    return (
        <Header className={styles.tt}>
            <div>
                
            </div>
        </Header>
    )
}

export default Header;